<div class="card mb-4">
    <div class="card-header bg-info text-white">
        <h5 class="mb-0">Detailed Usage Data</h5>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Measurement</th>
                        <th>Usage (ml)</th>
                        <th>Saturation (level)</th>
                    </tr>
                </thead>
                <tbody>
                    {% for entity in comparison.entities %}
                        {% for result in entity.results %}
                        <tr>
                            <td>{{ entity.brand }} #{{ result.measurement_number }}</td>
                            <td>{{ result.result_value }} ml</td>
                            <td>{{ result.saturation }}</td>
                        </tr>
                        <tr>
                            <td colspan="3" class="small">
                                {{ result.note }}
                            </td>
                        </tr>
                        {% endfor %}
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>