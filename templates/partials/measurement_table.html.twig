<div class="card mb-4">
    <div class="card-header bg-info text-white">
        <h5 class="mb-0">Detailed Usage Data</h5>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Measurement</th>
                        <th>Usage (ml)</th>
                        <th>Saturation (level)</th>
                    </tr>
                </thead>
                <tbody>
                    {% for product in comparison.products %}
                        {% for result in product.results %}
                        <tr>
                            <td>{{ product.brand }} #{{ result.measurement_number }}</td>
                            <td>{{ result.result_value }} ml</td>
                            <td>{{ result.saturation }}</td>
                        </tr>
                        {% endfor %}
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>